# PMF Model Review Package — 2026-05-08

## Run status — 2026-05-08 — snapshot `pre_close`

**PROVISIONAL** — safe to use, with the caveats below

- props: **340**
- books: **14**
- market coverage: **full**
- injury freshness: **fresh**
- role provenance: `derived_from_projected_minutes`: 340
- model: `12676b8#phase10c`

### Caveats

Full detail (including the `required_to_resolve` field for each blocker) is in `wizard_of_odds/run_manifest.json`.

- `lineup_unconfirmed` — No confirmed-lineup rows were present. This is acceptable for morning/provisional outputs but not final lock outputs.

---

Generated by `scripts/build_daily_pmf_delivery.py`. See `docs/daily_pmf_delivery_spec.md` for the row schema.

## What's in this package

- `01_START_HERE.html` — read first.
- `02_MODEL_REVIEW_OVERVIEW.html` — slate summary, model version, quality flags.
- `03_PMF_DISTRIBUTION_VIEWER.html` — visual histogram of every PMF.
- `04_PROP_SUMMARY.{csv,parquet}` — one row per (player, stat) with mean/median/mode/p0.
- `05_FULL_PMF_WIDE.{csv,parquet}` — `04_*` plus `pmf_json` and `p_ge_1 … p_ge_20`.
- `06_OUTCOME_LEVEL_PROBABILITIES.{csv,parquet}` — long form, one row per (player, stat, k).
- `machine_readable/` — exact same data, programmatic consumption.

Rows: **340**.

## Hard guarantee — model-only, never anchored

PMFs in this package are the **canonical model-only PMFs**. They are NOT market-anchored. No PMF probability has been adjusted to fit a book line. Market data (when present at all) lives in the separate Wizard of Odds package as a side-by-side reference; PMFs there are identical to the PMFs here.

## TOV status

TOV PMFs (when emitted by the slate) are produced by the current production Phase 8 calibrators. **No Phase 10D / 10D.2 TOV overlay is applied** — those overlays did not pass independent validation. See `docs/phase11_tov_structural_refit_plan.md` for the next move.
